@page "/learnblazor/parentcomponent"

<h3>Parent/Child Relation</h3>

<CascadingValue Value="@MessageForGrandChild" Name="message">
    <CascadingValue Value="@LuckyNumber" Name="luckyNumber">
        <ChildComponent Title="This is a message from the parent!" OnButtonClick="@ShowMessage">
            Render fragment from parent!
        </ChildComponent>
    </CascadingValue>
</CascadingValue>

<div>
    @MessageText
</div>

<AnotherChildComponent placeholder="Placeholder From Parent" maxlength="5" />

@code {
    public string MessageText = "";
    public string MessageForGrandChild = "This message is from your parent (ParentComponent)";
    public string LuckyNumber = "7";

    private void ShowMessage()
    {
        if (string.IsNullOrWhiteSpace(MessageText))
            MessageText = "Button from Child Component";
        else
            MessageText = "";
    }
}
